services:
  db:
    image: "mysql:latest"
    environment:
      MYSQL_DATABASE: "studentDetails"
      MYSQL_ROOT_PASSWORD: "admin@123"
    ports:
      - '8080:8080'

  pythonapp:
    build: ./server/
    command: python3 ./Login.py
    depends_on:
    	- mysql
